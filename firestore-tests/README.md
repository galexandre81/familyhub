# firestore-tests

Security-rules unit tests for `../firestore.rules`. Requires Java (Firestore emulator).

Run: `firebase emulators:exec --only firestore "npm test"` (from this folder, with deps installed).
